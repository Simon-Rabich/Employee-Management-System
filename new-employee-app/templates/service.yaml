apiVersion: v1
kind: Service
metadata:
  name: {{ include "new-employee-app.fullname" . }}
  labels:
    {{- include "new-employee-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "new-employee-app.selectorLabels" . | nindent 4 }}
