apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "new-employee-app.fullname" . }}-test-connection"
  labels:
    {{- include "new-employee-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "new-employee-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
